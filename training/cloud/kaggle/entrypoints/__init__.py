"""training/cloud/kaggle/entrypoints — Thin, provider-specific Kaggle kernel
entry scripts. These contain NO training logic of their own - see each
module's docstring for what it actually does (clone the pinned commit,
install dependencies, invoke the real training script unchanged).
"""
